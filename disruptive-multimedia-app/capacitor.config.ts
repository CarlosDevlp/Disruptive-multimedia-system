import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'disruptive-multimedia-app',
  webDir: 'build'
};

export default config;
